# V38.3 True Single Accordion

No SQL required.

Fixes:
- Replaces Product Hub with a strict controlled React accordion
- Only one category can be open at a time
- Opening a new category closes the old one
- Removes all native <details> behavior
- Uses clean + / − controls
