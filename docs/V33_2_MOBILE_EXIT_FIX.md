# V33.2 Mobile Exit Fix

No SQL required.

Fixes:
- Adds mobile-visible hamburger and bell quick-action buttons
- Adds visible X close buttons to menu and notification overlays
- Allows tapping outside overlays to close them
- Makes overlays fit mobile viewport and scroll internally
- Adds a visible sticky X to spot detail modal where supported
