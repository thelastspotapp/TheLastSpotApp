# V60.8 Recovery Compile-Safe Build

No SQL required.

Purpose:
- Returns to the Archive21 compile-fixed source.
- Does not include the unsafe V60.7 navigation branch that produced `Unexpected token main`.
- Use this to get Vercel green again before continuing navigation or TLS! rebrand work.

Known remaining UX items after this recovery:
- Mobile landscape/pill navigation still needs a safer isolated fix.
- Desktop Contact may still need routing cleanup.
- TLS! rebrand has not been applied.
