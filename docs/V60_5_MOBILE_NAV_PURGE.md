# V60.5 Mobile Navigation Purge

No SQL required.

Fixes:
- Removes legacy mobile Explore/Host pill behavior.
- Replaces mobile navigation with one true portrait menu inside the real Nav component.
- Keeps bottom action bar: Find / Reserve / Checkout / Earn.
- Desktop keeps Explore + Host and adds How It Works + Contact.
- Adds viewport metadata to prevent landscape dependency.
- Does not include TLS! rebrand yet.
